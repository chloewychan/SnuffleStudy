# Copy to .env and fill in real values. See .env's comments for which vars are
# safe to expose client-side (WXT_-prefixed) vs Node-only (unprefixed).
WXT_SUPABASE_URL=
WXT_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
