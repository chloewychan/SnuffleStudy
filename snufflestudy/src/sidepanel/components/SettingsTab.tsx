// v3.3 Task 1: TempPasscodePanel and UnlockRequestPanel (previously composed here) have moved to
// FriendsTab.tsx, below its existing StudyRoomPanel/FriendGroupPanel. This tab is deliberately
// emptied as a placeholder - Task 7 rebuilds it into a real sub-navigated Settings/Account/
// Friends/History surface, per the V3.3 Implementation Plan.
export function SettingsTab() {
  return <div className="sp-tab-content sp-settings-tab" />;
}
